# 2026-08-15 工作记录

## 亲子·学习闯关难度与题型大改（v41）
玉婷反馈：学习闯关太易赚星（孩子专去刷分），且口算/识字题型单一。本次改动：

- **连对给星 5★→1★**：`renderStudyView` 中 `kidsStudy.combo % 5 === 0` 时 `awardStars(5,...)` 改 `awardStars(1,...)`、`stars += 1`、toast 改 `+1★`。治"刷分"。
- **口算→数学（扩题型）**：`genQuestion` math 分支由单一 +−× 改为随机混合 4 类——calc(算)+compare(比大小 ○，选项 > < =)+blank(填括号)+word(二年级应用题)。标签 口算→数学。
- **识字→语文（扩题型+扩词库）**：pinyin 分支改为随机混合 3 类——toPinyin(看字选拼音，原)+toChar(看拼音选汉字)+antonym(选反义词，用新表 STUDY_ANT)。`STUDY_HANZI` 由 84 字扩到约 230 字（新增家人/反义/自然/物品/动物等）；新增 `STUDY_ANT`（34 对反义词）。`renderStudyView` 加 toChar/antonym 渲染分支；女巫小屋 `renderWitchStudy` 的 else 分支天然兼容新题型(显示题干+选项)。标签 识字→语文。
- **四处标签同步**：`SUBNAME`(学习闯关)、`SUBNAME2`(女巫小屋)、女巫魔法元素提示、学习卡提示，均 口算→数学、识字→语文。
- index.html 升 `app.js?v=41`（强清缓存）。沙箱禁用 node/python，靠 Read 逐段核对括号配平（genQuestion/renderStudyView/词库数组均闭合）。

注意：数学/语文题量扩充后"题海"更足；连对5题仅得1★，整体刷星难度显著提升。

## 亲子·宠物"疏忽回退"机制（v42）
玉婷想要：宠物太久不照顾→生病→等级(care)往回退→一直不管退回到 🥚 蛋。原代码 care 只增不减（每次互动 +1，门槛 12/36/72 对应 🐤🐔🦚），四项指标衰减会生病但不降等级。本次：

- **loadPet 加 care 天数回退**：在原有四项衰减(按 lastCare 小时制 drop=floor(hrs*5))之后，新增基于"今天是否照顾过"的回退——`dayOf()` 取本地年月日；`caredToday = dayOf(lastCare)===dayOf(now)`；`lastDecayDay` 记录上次处理日，每天仅处理一次；若今天没照顾且 care>0 则 `care = max(0, care-8)`（约9天从凤凰退回蛋，温和）。照顾当天(caredToday)不回退，保护正常日常。
- **生病提示加等级下降警告**：宠物卡生病块追加"⚠️ 太久不照顾，等级会每天下降，一直不管会退回 🥚 蛋宝宝"。降级后 emoji 由 stage=(care>=72?3:...) 自动随 care 变小，视觉即体现。
- index.html 升 `app.js?v=42`（强清缓存）。

边界：现有四项衰减在 drop>0 时重置 lastCare=now，但仍落在"今天"内，caredToday 仍 true，不会误回退；连续多天不开则每日 -8 直至 0。

## 亲子·五项体验优化（v43）
玉婷回复"12345"= 全部采纳上一轮提出的5个优化点，均在 renderKids 内：

1. **宠物主卡加"已同住小伙伴"墙（修孩子反馈 bug）**：原"已搬来同住"只在朋友小窝视图显示，宠物主卡漏了。宠物卡 pc.innerHTML 末尾（朋友小窝按钮后）用 IIFE 取 `pet.friends` 中 moved 项，渲染"🏡 已同住的小伙伴：emoji 名字"墙（未搬来则不显示）。
2. **学习闯关加"复习错题"入口 + 复习视图**：`renderStudyView` 开头加 `if(kidsStudy.mode==='review'){renderReviewView();return;}`（防取 undefined subject）；科目按钮下加红色虚线"📕 复习错题"按钮(stReview→kidsStudy={mode:'review'})；新增 `renderReviewView()` 读 kids 仓库 type:'mistake'（按 date 倒序），逐条显示题干+正确答案+「✓ 已掌握」(删该条)，空则提示"错题本空空"。错题本闭环打通。
3. **妈妈后台 tab 加待审核角标**：顶部 tab 渲染前 `pd=checks.filter(c=>c.status==='pending').length`，"👩 妈妈后台"后加 `⏳N`（无则不显示）。
4. **愿望兑换记录日期**：兑换时 `w.redeemedAt = d`（todayStr）；宝贝主页/妈妈后台两处"已兑换"后追加" · 日期"（replace_all 一次改两处）。
5. **屏幕时间加确认 + 今日已用**：屏幕卡片显示"已用 X 分"（取 screenInfo().used）；`.scr-use` 点击弹 `confirm('确定用掉 N 分钟屏幕时间吗？')` 确认后才扣减。
- index.html 升 `app.js?v=43`（强清缓存）。结构逐段 Read 核对闭合（renderReviewView 函数、IIFE 墙均闭合）。
