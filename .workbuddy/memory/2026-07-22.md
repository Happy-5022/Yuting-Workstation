# 2026-07-22 工作记录

## 玉婷个人工作台 PWA（已完成首版）
- 需求：温暖自然风 + 左侧抽屉菜单 + 个人创作/学习工作台。9 模块：每日计划/选题灵感/爆款二创/内容复盘/备忘录/尤克里里/英语/越南语/古法健身操。
- 技术：纯静态 PWA（无构建步骤）。vanilla JS + IndexedDB 离线存储；Service Worker 缓存外壳；manifest + apple-touch-icon 支持加到 iPhone 主屏幕。
- 文件：index.html / css/style.css / js/db.js（IndexedDB 封装）/ js/app.js（抽屉导航+路由+9 模块）/ sw.js / manifest.webmanifest / 图标(192/512/apple-touch)。工具：tools/gen-icons.js（纯 node 生成 PNG 图标）。
- 部署：CloudStudio 静态站，链接 https://1708efd524ae4802a96182e8c05ce76c.app.codebuddy.work （入口 index.html）。
- 备注：越南语、古法健身操是按「学习打卡+统计」套路做的第一版，用户说可后续调整。已加「导出备份」按钮（json 下载）防数据丢失。

## 尤克里里「每日练习」改为练习清单+打勾（22:00 后）
- 原「记录今天练习」（弹窗填阶段/分钟）改为：长期练习项清单（meta ukeExercises，可增删）+ 每日逐条打勾（uke 表 type:'daily' 按天存 checks）+ 顶部今日完成率进度条。借用 .task/.check 样式。已部署生效。

## 大改造：参考「胡楚靓工作台」（22:14 后，玉婷发来朋友截图参考）
- 玉婷要求全部照着改，优先级：每日计划 > 选题灵感 > 爆款二创 > 尤克里里 > 英语 > 视觉统一
- **已完成第一批（3个核心模块）**：
  1. **每日计划** → 分组卡片式：顶部两个统计卡（待完成/完成率）+ 「个人日常」「创作任务」两分区 + 每条任务独立卡片（✓勾选 + 任务名 + 标签 + ×删除）+ 分区底部添加按钮
  2. **选题灵感** → 编号卡片列表：每日灵感来源卡（更新时间+条数）+ 编号圆圈(1.2.3.) + 分类标签（思考/搞笑/化妆等8类）+ 详细说明 + ▶抖音/B站跳转按钮 + 收藏/删除
  3. **爆款二创** → tab切换(热点榜/挑战榜) + 分类筛选行(8类) + 分析卡（标题+类型标签+来源+为什么适合二创+改编角度+个人笔记）+ 操作按钮组（去抖音/加入任务/存为灵感/换视频/删除）
- 新增 CSS：.stat-card/.stat-big/.sec-block/.task-card/.idea-card/.hot-card 等全套新样式
- **待做第二批**：尤克里里（按阶段自动推荐+技能点详情+视频入口）、英语（BBC文章库+双语对照+音频播放器）、全局视觉统一

## 第二批改造全部完成（22:41 后，"接着做"）
玉婷说"接着做"，完成剩余 3 个任务：
1. **尤克里里大改**（任务#12）：进阶路线卡片显示「已掌握X/5阶段｜当前第N阶段」+ 重置按钮；今日练习按当前阶段自动推荐 daily 项（每项标题+分钟+说明+可打勾）；阶段技能树可展开，每个技能点卡片含目标/过关标准/3个视频入口（B站教学/B站更多/抖音跟练）；「✓标记掌握」推进阶段；下一阶段🔒未解锁。UKE_STAGES 数据扩展为每个阶段含 skills[] 和 daily[]。
2. **英语升级**（任务#13）：EN_ARTICLES 5篇 BBC 文章库（标题/中文/词数/时长/词汇量/正文段落/重点词）；列表点击进详情；详情页有 📖显示译文/🎓单词训练/🔄重置 + **真实语音播放器**（用浏览器 SpeechSynthesis 朗读英文，倍速滑块1.0x、进度条、⏮▶⏭🔁循环按钮、点击段落单独朗读）；已听次数按文章存 meta enHeard_*。
3. **全局视觉统一**（任务#14）：所有 .card 和 .item 加 1.5px 边框统一卡片风，与参考图一致。
- 全部已部署生效。新增 CSS：.uke-route/.stage-row/.skill-card/.art-card/.player 等。
- 注意：英语播放器依赖浏览器语音合成，iPhone Safari 需点击播放按钮触发（已满足）；真实音频文件未接入，用 TTS 替代。
- 待补（用户可能要）：导入备份、想法/爆款的数据可视化、模块细节微调。

## 配色微调（21:43 后）
- 用户要求：米黄色背景 + 橄榄绿主色。已改 css/style.css 的 :root 变量（--bg:#F5ECD7 米黄 / --primary:#708238 橄榄绿 / --accent:#8DA65D 浅橄榄点缀），并同步抽屉渐变、顶栏背景、悬浮按钮阴影。已重新部署生效。

## 选题灵感：接入「每日9点·热点选题灵感」自动化（23:47 后）
- 用户要求：选题每天自动更新，来自已有的自动化「每日9点·热点选题灵感」（每日9点跑，ACTIVE），每条附跳转链接点开即搜相关作品。
- 实现"桥"方案：新增 `data/daily-ideas.json`（自动化每天写、推上线）；PWA 的 renderIdeas 打开时 `fetch('./data/daily-ideas.json?v=DATE')` 取当日选题显示；取不到（离线）则 `localDaily()` 用 mulberry32(日期种子) 从 IDEAS_BANK 兜底生成 10 条（同日稳定、隔天自动换）。
- 每条选题下方三个跳转按钮：抖音搜索 / B站搜索 / 百度搜索（openLink 跳对应搜索 URL）。另加「☆收藏」→存 DB 'ideas'；下方「我的灵感」区展示收藏/手动添加项（可删、可搜）。新增 🔄立即刷新 按钮。
- 自动化已 update：prompt 改为"WebSearch 查热点→生成10条 JSON→Write 到 data/daily-ideas.json→workbuddy_cloudstudio_deploy 推上线"。注意该自动化 cwds 指向另一工作区(2026-07-22-23-35-41)，但 prompt 用绝对路径写文件与部署，不受影响。
- 前期小改：个人日常说明"普拉提"→"健身操"（仅文案，无种子数据）。
- 已部署生效；daily-ideas.json 与 loadDaily/localDaily 逻辑线上验证通过。

## 爆款二创：热点榜每日自动更新 + 挑战榜#话题跟拍（23:54 后 / 次日0点）
- 需求：热点榜每日自动更新热点资讯；挑战榜=带#话题、可直接跟拍的内容，点「去参与挑战」找灵感、「交作业」记录。
- 重写 renderHot：两个 tab（热点榜/挑战榜），共用 `data/daily-hot.json`（自动化每天写 hot[5]+challenges[5]，date=今天才采用，否则本地兜底 HOT_BANK/CHAL_BANK + mulberry32 日期种子）。分类筛选保留。
  - 热点卡：标题+分类+来源+热度🔥 + 「可以这样蹭」说明；按钮：去抖音看/B站看/加入任务(category:create,'蹭热点：')/存为灵感(存 DB hot type:'hot')。
  - 挑战卡(.chal-card 绿左边框)：#话题(主色)+分类+参与量 + 怎么拍/为什么适合；按钮：▶去参与挑战·找灵感(跳抖音搜话题去#)/✍交作业(加任务 '交作业：参加 #XX 挑战')/存为灵感(type:'chal')。
  - 「我收藏的」区按当前 tab 过滤(type chal/非chal)，可加入任务/删除/手动收藏。
- 自动化 prompt 再更新：现在每天同时产出 sel3份→写 daily-ideas.json + daily-hot.json 两个文件→部署。
- 新增 CSS：.chal-card / .chip.hot。已部署线上验证通过。
- 注意：Write daily-hot.json 第一次误写进回复文本没执行，第二次补写成功——以后建文件务必用 Write 工具真实调用。
