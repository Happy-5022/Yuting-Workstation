# 2026-08-11 工作日志

- 玉婷反馈亲子模块「界面不舒服」，给出 4 类改进建议：①信息架构拆「宝贝主页/妈妈后台」两分页（管理按钮移后台，孩子只看到打卡+宠物）；②视觉童趣化（暖色马卡龙、大圆角大 emoji、打卡撒花动画）；③加「今日进度条 + 连续打卡🔥」成就感锚点；④两个宠物（我的小宠物 / 待做的女巫魔法屋）将来合并为「宠物小屋」避免混淆；另建议妈妈后台加数据看板（打卡率/星星趋势）。用户尚未决定动手改哪些，倾向先看草图。注：「女巫魔法屋」小游戏上一轮要求做但被部署指令/本问题打断，仍未实现。

- 【每日9点自动化】生成并写入 data/daily-ideas.json（10 条选题）、daily-hot.json（5 热点 + 5 挑战）、daily-english.json（5 篇 BBC 原创摘要），date=2026-08-11，JSON 校验通过。commit eae24ad；git push 失败（`Recv failure: Connection was reset`，沙箱网络问题，非凭据），积压 1 个待推 commit，需玉婷自己在电脑上 `git push`。CloudStudio 备用部署成功：https://1708efd524ae4802a96182e8c05ce76c.app.codebuddy.work
  注：08-05～08-10 该自动化未执行，数据文件断更 6 天，今天已补上。

- 按"先做见效快的预览版"落地：在 `renderKids` 内新增 ①`kidsStyle`（`<style>` 拼进 `view.innerHTML` 开头，作用 .card 暖色圆角+绿按钮+进度条类，`!important` 覆盖全局，切模块即消失不污染）；②`streakDays()` 连续打卡统计（按 done 日期去重向前数，今天没打从昨天起算）；③`burst()` 撒花动画（点击位置飞 emoji 1.2 个，0.9s 淡出移除）；④`paint()` 顶部加「今日进度 X/Y + 🔥连续 N 天」进度条（doneTodayCount/progPct 计算）；⑤任务"完成"按钮 onclick 传事件坐标触发 burst。index.html 升 `app.js?v=29`。纯前端离线，未动 db.js/导出清单。

- 治本大改：亲子模块拆「👧 宝贝主页 / 👩 妈妈后台」两页。在 `renderKids` 内加 `let kidsPage='home'`，`paint()` 顶部渲染 `.kids-tabs` 双按钮（带 `data-page`、点击切 `kidsPage` 重绘）；其余内容按 `kidsPage` 分支：主页=顶部卡(星星+进度条+屏幕剩余+待审核提示)+今日任务打卡+我的愿望(兑换)+我的小宠物；后台=汇总+📊本周数据看板(打卡率/本周打卡/本周获得+近7天打卡柱状图 `.kids-chart`)+⏳待审核(批准/驳回)+🎁愿望管理(删除)+📋任务管理(删除)+⚙️设置(切审核)。原"加任务/加愿望/设置"按钮从主页移入后台；原待审核渲染已删（改在后台）。各分支的按钮事件绑定也包进对应 `if`，避免空引用。index.html 升 `app.js?v=30`。未动 db.js/导出清单（kids 仓库沿用）。

- 【调研】玉婷要在亲子模块叠「学科学习闯关」（二年级口算/九九乘法/识字/英语启蒙），本会话 WebSearch + 子代理均故障，基于行业通用做法+前期亲子调研给方案：①口算=算法随机出题+计时/连对奖励+错题本；②乘法=81句随机抽考(顺序背→乱序→填空→抢答)；③识字=内置字库JSON(字+拼音+组词)+听音选字/描红/闯关地图(洪恩式)；④英语=词库(emoji代图)+浏览器原生 SpeechSynthesis 离线发音(听音选图/跟读/自然拼读)，不依赖联网TTS。通用机制：关卡进度条+连对徽章+即时正反馈(撒花)+错题本。嵌入手台：宝贝主页加「📚学习闯关」入口卡→独立学习子页(顶部选科目+答题卡+关卡进度)；答对赚星接现有 awardStars 闭环；妈妈后台加学习数据(正确率/今日题数)。题库策略：口算/乘法可算法生成(零资源)，识字/英语需内置精简 JSON 词库。待玉婷定先动手哪类（联网恢复后可补真实产品佐证）。

- 亲子模块落地「📚 学习闯关」全四科（按"按你想法帮我增加吧"一次性做）：renderKids 内加 `let kidsStudy=null` + 常量 `STUDY_HANZI`(24字:字+拼音+组词)/`STUDY_ENG`(24词:英文+中文+emoji) + 工具 `rnd/pick/shuffle/pickN/makeOpts/speak`(浏览器原生 SpeechSynthesis 离线发音) + `genQuestion(sub,done)`(口算=算法随机按难度/乘法=81句随机抽/识字=给汉字选拼音/英语=听音选图) + `renderStudyView()`；`kidsStyle` 提至 renderKids 级供复用；paint 内 `if(kidsStudy){renderStudyView();return;}` 切到独立学习页。宝贝主页加「📚 学习闯关」入口卡(stEnter→kidsStudy={subject:'math',done:0,got:0,q:null,answered:false})；妈妈后台本周数据加「本周错题」misCount(读 kids 仓库 type:'mistake')。答对 `awardStars(1★)` 接星星闭环+`burst`撒花，答错存 `kids` 仓库 type:'mistake'。英语发音用原生 SpeechSynthesis(离线可用，不依赖联网TTS)。index.html 升 `app.js?v=31`。未动 db.js/导出清单（mistake 复用 kids 仓库）。

- 词库扩充 + 英语听音选图改进（玉婷要求）：①`STUDY_HANZI` 24→88 个二年级常用字(字+拼音+组词)；②`STUDY_ENG` 24→100 词(英文+中文+emoji，新增水果/动物/身体/食物/文具/数字/颜色/动作/天气/家人/地点等)；③英语「听音选图」加**发音人下拉选择**：renderKids 内加 `let kidsVoice=localStorage('kidVoice')` + `loadVoices()`(过滤 lang 以 en 开头的语音) + `refreshVoiceSelect()` + `speechSynthesis.onvoiceschanged` 监听(异步语音就绪后自动填充下拉)；`speak()` 改用所选 voice；select 的 `onchange` 存回 localStorage 并立即试读。④英语选项改为「emoji 图 + 中文意思」双行显示（`data-w` 存英文单词，`o.zh` 显示中文），答错提示 `正确答案：apple（苹果）`。pinyin/calc 仍用 `data-o` 字符串匹配，onclick 统一 `b.dataset.w||b.dataset.o`。index.html 升 `app.js?v=32`。
