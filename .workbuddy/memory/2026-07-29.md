# 2026-07-29 工作日志

## BBC 随身英语「离线兜底」复现 bug 修复
- 现象：用户反馈英语模块 BBC 随身英语"又开始离线兜底"。
- 根因：与 07-28 选题灵感同一类死规矩——`loadDailyEn`(app.js 第2013行) 要求云端 `data/daily-english.json` 的 `date === todayStr()` 才用；文件日期停在 2026-07-28，今天 07-29 跨天即判过期 → 退回 `localDailyEn()` 内置5篇（显示"离线兜底"）。昨天能用是因为日期对得上。
- 修复：去掉 `j.date === todayStr()` 强判，改为"文件有效（有 articles 数组且非空）即用"；保留 `j._stale` 标记，meta 显示"非今日生成（点刷新可重载）"温和提示。fetch 的 `?v=` 由 todayStr() 改为 Date.now() 防缓存。
- 版本号：`index.html ?v=15`→`?v=16`；`sw.js CACHE yt-wb-v6`→`yt-wb-v7`（强制 PWA 刷新）。
- **待办**：用户需 push；手机划掉 PWA 重开查看。
- 提醒用户：内容更新依赖"每日9点自动化"真的在跑并生成新文件；现在是凌晨，今天9点的还没跑，显示的仍是 07-28 那5篇（正常，非bug）。若自动化长期不更新，内容不会自动变新。
