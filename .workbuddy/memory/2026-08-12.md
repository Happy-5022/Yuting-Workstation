# 2026-08-12

- 修复 v39「朋友小窝」线上报错：玉婷 iPhone 点「朋友小窝」后弹 `未捕获错误 @ app.js:4972:20`。根因：进入朋友视图后 `paintPetFriends` 只渲染 `#pfBack`/`#pfVisit-*`/`#pfParty`，不渲染宠物页的 `#petFeed`/`#petPlay`/`#petBath`/`#petSleep`/`#petName` 及学习/女巫入口；但 `renderKids` 绑定区直接 `$('#petFeed').onclick = ...`，元素不存在时报 `Cannot set properties of null`。修复：将宠物互动按钮、改名、学习闯关入口、女巫小屋入口的绑定全部改为 null 安全检查（与已有的 `cureBtn`/`pfBtn` 保持一致），即 `const x=$('#id'); if(x) x.onclick=...`。index.html 升 `app.js?v=40` 强清缓存。
